/**
 * Fortis APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  compositeAuthenticationProvider,
  customHeaderAuthenticationProvider,
} from './authentication';
import { Configuration } from './configuration';

export function createAuthProviderFromConfig(config: Partial<Configuration>) {
  const authConfig = {
    userId:
      config.userIdCredentials &&
      customHeaderAuthenticationProvider(config.userIdCredentials),
    userApiKey:
      config.userApiKeyCredentials &&
      customHeaderAuthenticationProvider(config.userApiKeyCredentials),
    developerId:
      config.developerIdCredentials &&
      customHeaderAuthenticationProvider(config.developerIdCredentials),
    accessToken:
      config.accessTokenCredentials &&
      customHeaderAuthenticationProvider(config.accessTokenCredentials),
  };

  return compositeAuthenticationProvider<
    keyof typeof authConfig,
    typeof authConfig
  >(authConfig);
}
