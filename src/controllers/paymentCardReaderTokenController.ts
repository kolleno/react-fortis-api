/**
 * Fortis APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core';
import {
  ResponsePaymentCardReaderToken,
  responsePaymentCardReaderTokenSchema,
} from '../models/responsePaymentCardReaderToken';
import { string } from '../schema';
import { BaseController } from './baseController';
import { Response401tokenError } from '../errors/response401tokenError';

export class PaymentCardReaderTokenController extends BaseController {
  /**
   * For initializing iPhone card readers for Apple Tap to Pay transactions
   *
   * @param productTransactionId   Product Transaction ID to be used to initialize the card reader
   * @return Response from the API call
   */
  async paymentCardReaderTokenRequest(
    productTransactionId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ResponsePaymentCardReaderToken>> {
    const req = this.createRequest('GET', '/v1/payment-card-reader-token');
    const mapped = req.prepareArgs({
      productTransactionId: [productTransactionId, string()],
    });
    req.query('product_transaction_id', mapped.productTransactionId);
    req.throwOn(401, Response401tokenError, 'Unauthorized');
    req.authenticate([{ userId: true, userApiKey: true, developerId: true }]);
    return req.callAsJson(responsePaymentCardReaderTokenSchema, requestOptions);
  }
}
