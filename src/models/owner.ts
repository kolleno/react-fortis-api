/**
 * Fortis APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  boolean,
  expandoObject,
  nullable,
  number,
  optional,
  Schema,
  string,
} from '../schema';

/** Array of merchant owners. */
export interface Owner {
  /** Owner's first name. */
  firstName: string;
  /** Owner's last name. */
  lastName: string;
  /** Owner's middle name. */
  middleName?: string | null;
  /** Owner's title. */
  title: string;
  /** Owner's date of birth. */
  dateOfBirth: string;
  /** Owner's street address. */
  addressLine1: string;
  /** Line 2 of owner's address. */
  addressLine2: string;
  /** Owner's address city. */
  city: string;
  /** Owner's address state/province. */
  stateProvince: string;
  /** Owner's address postal code. */
  postalCode: string;
  /** Owner's address country. */
  countryCode: string;
  /** Owner's full SSN. */
  ssn: string;
  /** Owner's ownership percent. */
  ownershipPercent: number;
  /** Owner's phone number. */
  phoneNumber: string;
  /** Owner's email address. */
  emailAddress: string;
  /**
   * Flag indicating whether this owner is the control owner. Maximum of 1 owner can be marked as control..
   * >Flag indicating whether this owner is the control owner. Maximum of 1 owner can be marked as control.
   * >
   */
  isController: boolean;
  /**
   * Flag indicating whether or not the owner is a signer for the business. Maximum of 1 owner can be marked as signer.
   * >Flag indicating whether or not the owner is a signer for the business. Maximum of 1 owner can be marked as signer.
   * >
   */
  isSigner: boolean;
  [key: string]: unknown;
}

export const ownerSchema: Schema<Owner> = expandoObject({
  firstName: ['first_name', string()],
  lastName: ['last_name', string()],
  middleName: ['middle_name', optional(nullable(string()))],
  title: ['title', string()],
  dateOfBirth: ['date_of_birth', string()],
  addressLine1: ['address_line_1', string()],
  addressLine2: ['address_line_2', string()],
  city: ['city', string()],
  stateProvince: ['state_province', string()],
  postalCode: ['postal_code', string()],
  countryCode: ['country_code', string()],
  ssn: ['ssn', string()],
  ownershipPercent: ['ownership_percent', number()],
  phoneNumber: ['phone_number', string()],
  emailAddress: ['email_address', string()],
  isController: ['is_controller', boolean()],
  isSigner: ['is_signer', boolean()],
});
