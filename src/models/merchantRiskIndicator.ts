/**
 * Fortis APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  expandoObject,
  number,
  optional,
  Schema,
  string,
} from '../schema';
import {
  DeliveryTimeframeEnum,
  deliveryTimeframeEnumSchema,
} from './deliveryTimeframeEnum';
import {
  PreOrderPurchaseIndEnum,
  preOrderPurchaseIndEnumSchema,
} from './preOrderPurchaseIndEnum';
import {
  ReorderItemsIndEnum,
  reorderItemsIndEnumSchema,
} from './reorderItemsIndEnum';
import {
  ShipIndicatorEnum,
  shipIndicatorEnumSchema,
} from './shipIndicatorEnum';
import { TransCharEnum, transCharEnumSchema } from './transCharEnum';

/** Contains purchase information */
export interface MerchantRiskIndicator {
  /**
   * Indicates shipping method chosen for the transaction. Merchants must choose the Shipping Indicator code that most accurately describes the cardholder's specific transaction. If one or more items are included in the sale, use the Shipping Indicator code for the physical goods, or if all digital goods, use the code that describes the most expensive item.
   * >01 - Ship to cardholder's billing address
   * >
   * >02 - Ship to another verified address on file with merchant
   * >
   * >03 - Ship to address that is different than the cardholder's billing address
   * >
   * >04 - "Ship to Store" / Pick-up at local store (Store address shall be populated in shipping address fields)
   * >
   * >05 - Digital goods (includes online services, electronic gift cards and redemption codes)
   * >
   * >06 - Travel and Event tickets, not shipped
   * >
   * >07 - Other (for example, Gaming, digital services not shipped, e-media subscriptions, etc.)
   * >
   * >08 - Pick-up and go delivery. Availble in EMV 3DS 2.3.1 and later
   * >
   * >09 - Locker delivery (or other automated pick-up). Availble in EMV 3DS 2.3.1 and later
   * >
   * >80 - PS-specific value (dependent on the payment scheme type)
   * >
   * >81 - PS-specific value (dependent on the payment scheme type)
   * >
   */
  shipIndicator?: ShipIndicatorEnum;
  /**
   * Indicates the merchandise delivery timeframe.
   * >01 - Electronic Delivery
   * >
   * >02 - Same day shipping
   * >
   * >03 - Overnight shipping
   * >
   * >04 - Two-day or more shipping
   * >
   */
  deliveryTimeframe?: DeliveryTimeframeEnum;
  /** For electronic delivery, the email address to which the merchandise was delivered. */
  deliveryEmailAddress?: string;
  /**
   * Indicates whether the cardholder is reordering previously purchased merchandise.
   * >01 - First time ordered
   * >
   * >02 - Reordered
   * >
   */
  reorderItemsInd?: ReorderItemsIndEnum;
  /**
   * Indicates whether Cardholder is placing an order for merchandise with a future availability or release date.
   * >01 - Merchandise available
   * >
   * >02 - Future availability
   * >
   */
  preOrderPurchaseInd?: PreOrderPurchaseIndEnum;
  /** For a pre-ordered purchase, the expected date that the merchandise will be available. Date format must be YYYYMMDD. */
  preOrderDate?: string;
  /** For prepaid or gift card purchase, the purchase amount total of prepaid or gift card(s) in major units (for example, USD 123.45 is 123). */
  giftCardAmount?: number;
  /** For prepaid or gift card purchase, the currency code of the card as defined in ISO 4217 except 955 - 964 and 999. */
  giftCardCurr?: string;
  /** For prepaid or gift card purchase, total count of individual prepaid or gift cards/codes purchased. */
  giftCardCount?: number;
  /**
   * Available starting in EMV 3DS 2.3.1.1.  Indicates to the ACS specific transactions identified by the Merchant.
   * >01 - Cryptocurrency transaction
   * >
   * >02 - NFT transaction
   * >
   */
  transChar?: TransCharEnum[];
  [key: string]: unknown;
}

export const merchantRiskIndicatorSchema: Schema<MerchantRiskIndicator> = expandoObject(
  {
    shipIndicator: ['ship_indicator', optional(shipIndicatorEnumSchema)],
    deliveryTimeframe: [
      'delivery_timeframe',
      optional(deliveryTimeframeEnumSchema),
    ],
    deliveryEmailAddress: ['delivery_email_address', optional(string())],
    reorderItemsInd: ['reorder_items_ind', optional(reorderItemsIndEnumSchema)],
    preOrderPurchaseInd: [
      'pre_order_purchase_ind',
      optional(preOrderPurchaseIndEnumSchema),
    ],
    preOrderDate: ['pre_order_date', optional(string())],
    giftCardAmount: ['gift_card_amount', optional(number())],
    giftCardCurr: ['gift_card_curr', optional(string())],
    giftCardCount: ['gift_card_count', optional(number())],
    transChar: ['trans_char', optional(array(transCharEnumSchema))],
  }
);
